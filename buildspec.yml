version: 0.2

phases:
  install:
    commands:
      - cp -r .aws/ ~/
      - pip install --upgrade pip
      - pip install -r requirements.txt
      - python json-pdf-app.py
      - npm init --yes
      - npm install -g serverless
      - npm install --save serverless-python-requirements
      - ls /home
      - serverless deploy --aws-profile default
